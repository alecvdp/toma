---
phase: 03-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/log_service.py
  - services/import_service.py
  - pages/daily_log.py
  - pages/import_export.py
  - tests/test_log_service.py
  - tests/test_export.py
  - tests/test_import.py
  - pyproject.toml
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "User can select a date range and see a multi-row history grid (dates as rows, items as columns)"
    - "User can click Export to CSV and download a CSV file of the displayed history"
    - "User can upload a CSV or Excel file and import log data into the database"
    - "Import validates columns against existing catalog items and warns about unrecognized columns"
    - "Import shows a preview before committing data"
  artifacts:
    - path: "services/log_service.py"
      provides: "get_logs_by_date_range and export_logs_csv functions"
      contains: "def get_logs_by_date_range"
    - path: "services/import_service.py"
      provides: "validate_import and import_logs functions"
      contains: "def validate_import"
    - path: "pages/daily_log.py"
      provides: "History section with date range picker"
      contains: "date_range"
    - path: "pages/import_export.py"
      provides: "Import/Export page with upload and download"
      contains: "file_uploader"
    - path: "tests/test_log_service.py"
      provides: "Tests for date range query"
      contains: "test_get_logs_by_date_range"
    - path: "tests/test_export.py"
      provides: "Tests for CSV export"
      min_lines: 15
    - path: "tests/test_import.py"
      provides: "Tests for import validation and upsert"
      min_lines: 30
  key_links:
    - from: "pages/daily_log.py"
      to: "services/log_service.py"
      via: "import get_logs_by_date_range"
      pattern: "get_logs_by_date_range"
    - from: "pages/import_export.py"
      to: "services/import_service.py"
      via: "import validate_import, import_logs"
      pattern: "validate_import|import_logs"
    - from: "pages/import_export.py"
      to: "services/log_service.py"
      via: "import get_logs_by_date_range for export"
      pattern: "get_logs_by_date_range"
---

<objective>
Add history browsing (DATA-02), CSV export (DATA-03), and CSV/Excel import (DATA-04).

Purpose: Users can browse past logs by date range, export their data to CSV, and import existing medication data from CSV or Excel files -- making the tracker clearly better than a spreadsheet.
Output: History section on daily log page, new Import/Export page, import_service.py, unit tests for all service functions.
</objective>

<execution_context>
@/Users/alec/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enhancements/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From services/log_service.py:
```python
def upsert_log_entry(log_date: str, item_id: int, dosage_taken: float | None, notes: str = "") -> None:
def build_log_grid(target_date: str) -> pd.DataFrame:
    # Returns single-row DataFrame: item names as columns, dosage as values
```

From services/item_service.py:
```python
def get_active_items():
    # Returns list[dict] with keys: id, name, category, default_dosage, dosage_unit, ...
```

From db.py:
```python
@contextmanager
def get_connection():
    # Yields sqlite3.Connection with row_factory=sqlite3.Row
```

From db.py (schema):
```sql
-- daily_logs table
UNIQUE(log_date, item_id)
-- items table
name TEXT NOT NULL UNIQUE
is_active INTEGER NOT NULL DEFAULT 1
sort_order INTEGER NOT NULL DEFAULT 0
```

From tests/conftest.py:
```python
@pytest.fixture
def test_db(tmp_path):
    # Provides isolated SQLite database per test
```

From tests/test_log_service.py:
```python
def _create_item(name, default_dosage=None, dosage_unit="mg", sort_order=0):
    # Helper to create test items
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: History, export, and import service layer + tests</name>
  <files>services/log_service.py, services/import_service.py, tests/test_log_service.py, tests/test_export.py, tests/test_import.py, pyproject.toml</files>
  <behavior>
    - Test: get_logs_by_date_range("2025-01-15", "2025-01-17") returns a DataFrame with dates as rows, items as columns, dosages as values
    - Test: get_logs_by_date_range with no logs returns empty DataFrame
    - Test: export_logs_csv returns UTF-8 encoded CSV bytes with correct headers
    - Test: validate_import with valid columns returns (True, [])
    - Test: validate_import with missing "date" column returns (False, ["Missing required 'date' column"])
    - Test: validate_import with unrecognized item columns returns (True, [warning])
    - Test: validate_import with NO matching items returns (False, ...)
    - Test: import_logs upserts entries correctly for matched items
    - Test: import_logs skips unrecognized item columns
  </behavior>
  <action>
**Step 1: Add openpyxl dependency**
Run `cd /Users/alec/git/toma && uv add openpyxl` to add the Excel engine dependency.

**Step 2: Write tests first**

Add to tests/test_log_service.py:
- `test_get_logs_by_date_range`: Create 2 items, log entries on 3 dates, query a 2-day range, assert DataFrame shape (2 rows, 2 columns), assert correct values and NaN for unlogged items.
- `test_get_logs_by_date_range_empty`: Query a range with no logs, assert empty DataFrame.

Create tests/test_export.py:
- `test_export_logs_csv`: Build a date range DataFrame, call `export_logs_csv(df)`, decode result, assert CSV has correct headers and values.

Create tests/test_import.py (import from services.import_service):
- `test_validate_import_valid`: DataFrame with "date" + matching item columns returns (True, []).
- `test_validate_import_missing_date`: DataFrame without "date" column returns (False, [error]).
- `test_validate_import_unrecognized_columns`: DataFrame with "date" + unrecognized column returns (True, [warning]).
- `test_validate_import_no_matches`: DataFrame with "date" + only unrecognized columns returns (False, [error]).
- `test_import_logs`: Create items, build import DataFrame, call import_logs, verify entries in database.
- `test_import_logs_skips_unknown`: Import DataFrame with unknown column, verify no error and known items imported.

**Step 3: Implement service functions**

In services/log_service.py, add:
```python
def get_logs_by_date_range(start_date: str, end_date: str) -> pd.DataFrame:
```
Follow the pattern from RESEARCH.md: LEFT JOIN items->daily_logs with date BETWEEN, pivot_table with index="log_date", columns="item_name", values="dosage_taken", aggfunc="first", sort_index(). Only include is_active=1 items. Order columns by sort_order then name.

```python
def export_logs_csv(df: pd.DataFrame) -> bytes:
```
Simple wrapper: `df.to_csv().encode("utf-8")`. Index column should be labeled "date".

Create services/import_service.py with:
```python
def validate_import(df: pd.DataFrame, active_items: list[dict]) -> tuple[bool, list[str]]:
```
Check for "date" column. Compare remaining columns against item names. Return (is_valid, warnings/errors).

```python
def import_logs(df: pd.DataFrame, active_items: list[dict]) -> int:
```
For each row in df: extract date from "date" column. For each item column that matches an active item name, call upsert_log_entry(date, item_id, dosage_value). Return count of entries imported.

**Step 4: Run tests**
Verify all tests pass.
  </action>
  <verify>
    <automated>cd /Users/alec/git/toma && pytest tests/test_log_service.py tests/test_export.py tests/test_import.py -x -v</automated>
  </verify>
  <done>get_logs_by_date_range, export_logs_csv, validate_import, and import_logs all implemented and tested. openpyxl added as dependency. All 9+ new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: History section on daily log + Import/Export page</name>
  <files>pages/daily_log.py, pages/import_export.py</files>
  <action>
**Part A: Add History section to pages/daily_log.py**

After the existing Notes section (end of file), add a "History" section:

1. Add `st.divider()` then `st.subheader("History")`
2. Add `st.date_input("Date range", value=(date.today() - timedelta(days=7), date.today()), max_value=date.today(), key="history_range")`
3. Guard with `if len(history_range) == 2:` (per RESEARCH pitfall 3)
4. Call `get_logs_by_date_range(start.strftime("%Y-%m-%d"), end.strftime("%Y-%m-%d"))`
5. If DataFrame is not empty, display with `st.dataframe(history_df, use_container_width=True)`
6. Add export button: `st.download_button("Export to CSV", data=export_logs_csv(history_df), file_name=f"toma_log_{start}_{end}.csv", mime="text/csv")`
7. If empty, show `st.info("No log entries in this date range.")`
8. Import `timedelta` from datetime, import `get_logs_by_date_range, export_logs_csv` from log_service

**Part B: Create pages/import_export.py**

New Streamlit page for import functionality:

1. `st.title("Import / Export")`
2. **Export section:**
   - `st.subheader("Export")`
   - Date range picker for export range
   - When range is complete, fetch data and show download button
3. **Import section:**
   - `st.subheader("Import")`
   - `st.file_uploader("Upload CSV or Excel file", type=["csv", "xlsx"], key="import_file")`
   - When file uploaded:
     - Read with `pd.read_csv()` or `pd.read_excel(engine="openpyxl")` based on extension
     - Call `validate_import(df, get_active_items())`
     - If invalid, show `st.error` with reasons
     - If valid with warnings, show `st.warning` for each warning
     - Show `st.dataframe(df.head(10))` as preview
     - Show summary: "Will import X entries for Y items"
     - Add `st.button("Confirm Import")` -- on click, call `import_logs(df, get_active_items())`, show `st.success(f"Imported {count} entries")`
   - Import from services.import_service and services.item_service

**Register the page:** Check if there's a multipage app setup. The project uses Streamlit's pages/ directory convention, so creating the file in pages/ is sufficient.
  </action>
  <verify>
    <automated>cd /Users/alec/git/toma && pytest tests/ -x -q</automated>
  </verify>
  <done>Daily log page has a History section with date range picker, history grid display, and CSV export button. Import/Export page exists with file upload, validation preview, and confirmed import. All tests pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q` passes (all existing + new tests)
- `get_logs_by_date_range` returns correct multi-row DataFrame
- `export_logs_csv` produces valid CSV bytes
- `validate_import` correctly validates columns against catalog
- `import_logs` upserts entries for matched items
- Daily log page shows history grid with date range selection
- Import/Export page handles CSV and Excel uploads with validation
</verification>

<success_criteria>
- Date range history shows correct multi-day grid on daily log page
- CSV export downloads with correct data and filename
- CSV and Excel import works with column validation and preview
- Unrecognized columns are warned about, not silently dropped
- Import requires confirmation before writing to database
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhancements/03-02-SUMMARY.md`
</output>
