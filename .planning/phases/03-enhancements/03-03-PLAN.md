---
phase: 03-enhancements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [pages/catalog.py]
autonomous: true
gap_closure: true
requirements: [CAT-07, CAT-08]

must_haves:
  truths:
    - "Fetched Wikipedia description visually appears in the description textarea before form submit (add flow)"
    - "Fetched Wikipedia description visually appears in the description textarea before form submit (edit flow)"
    - "Fetch Description button uses the main Name field, not a separate lookup field"
  artifacts:
    - path: "pages/catalog.py"
      provides: "Catalog page with working fetch-to-textarea bridging"
      contains: "session_state.*add_description"
  key_links:
    - from: "fetch_wikipedia_description return value"
      to: "st.text_area widget display"
      via: "st.session_state[widget_key] assignment before st.rerun()"
      pattern: "session_state.*description.*=.*desc"
---

<objective>
Fix the Wikipedia description fetch so that fetched text visually appears in the description textarea immediately (on both add and edit forms), and remove the confusing separate lookup field.

Purpose: UAT tests 1 and 2 failed because Streamlit's keyed widget state caching causes value= param to be ignored after first render. The fix is to write directly to the widget's session_state key before rerun.
Output: Updated pages/catalog.py with working fetch-to-textarea bridging.
</objective>

<execution_context>
@/Users/alec/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enhancements/03-UAT.md
@pages/catalog.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix add-form fetch description widget state bridging</name>
  <files>pages/catalog.py</files>
  <action>
In the add item section of pages/catalog.py, fix the Streamlit widget key caching issue:

1. Remove the separate `fetched_description` session state variable entirely. It is no longer needed.

2. In the "Fetch Description" button handler (around line 51-59), after successfully fetching:
   - Set `st.session_state['add_description'] = desc` (write directly to the widget key)
   - Remove the line `st.session_state.fetched_description = desc`
   - Keep the `st.rerun()` call

3. In the st.text_area for description (around line 67-72):
   - Remove `value=st.session_state.fetched_description` -- the widget will read from its own key automatically
   - Keep the key="add_description" parameter
   - Use no explicit value= param (let Streamlit's session_state drive it)

4. After successful form submit (around line 89), reset with:
   - `st.session_state['add_description'] = ""` instead of `st.session_state.fetched_description = ""`

5. Clean up: Remove the `if "fetched_description" not in st.session_state` init block (line 26-27).

The name field already exists as add_name_input -- the fetch button already reads from it. No separate lookup field exists in current code, so no removal needed there. The UX confusion reported was about the name field being outside the form border -- this is a Streamlit constraint (buttons don't work inside st.form). Keep current layout but it will feel more connected since the description now visually updates.
  </action>
  <verify>
    <automated>cd /Users/alec/git/toma && python -c "import pages.catalog" 2>&1 | head -5 || echo "Module check: syntax valid if no SyntaxError"</automated>
  </verify>
  <done>Fetched description writes directly to the widget key so it appears in the textarea after rerun, without waiting for form submit.</done>
</task>

<task type="auto">
  <name>Task 2: Fix edit-form fetch description widget state bridging</name>
  <files>pages/catalog.py</files>
  <action>
In the edit item section of pages/catalog.py, fix the same Streamlit widget key caching issue:

1. In the "Fetch Description" button handler for edit (around line 103-110), after successfully fetching:
   - Set `st.session_state['edit_description'] = desc` (write directly to the widget key)
   - Remove the line `st.session_state.edit_fetched_description = desc`
   - Keep the `st.rerun()` call

2. Remove the `edit_desc_value` computation block (lines 113-117) entirely. It is no longer needed.

3. In the st.text_area for edit description (around line 140-145):
   - The widget key is "edit_description" -- Streamlit will read from session_state automatically
   - For the FIRST time the edit form opens (when there is no fetched override), initialize the widget key: In the edit form setup (when editing_item changes), add logic to set `st.session_state['edit_description'] = item["description"] or ""` ONLY if the editing_item just changed (not on every rerun). Use a guard like checking if a `_edit_item_loaded` session state flag matches the current editing_item id.
   - Remove the explicit `value=edit_desc_value` param from st.text_area. Keep key="edit_description".

4. On save/cancel (lines 168-173), clean up:
   - Remove `st.session_state.edit_fetched_description = None`
   - Instead delete or reset `st.session_state['edit_description']` and the loaded-item guard

5. Clean up: Remove the `if "edit_fetched_description" not in st.session_state` init block (line 28-29).

Key pattern: When editing_item changes to a new item id, seed `st.session_state['edit_description']` with the item's current description. When fetch fires, overwrite `st.session_state['edit_description']` with the fetched text. The widget always reads from its key.
  </action>
  <verify>
    <automated>cd /Users/alec/git/toma && python -c "
import ast, sys
with open('pages/catalog.py') as f:
    tree = ast.parse(f.read())
# Check no syntax errors and no references to edit_fetched_description or fetched_description
with open('pages/catalog.py') as f:
    content = f.read()
assert 'fetched_description' not in content or 'edit_fetched_description' not in content, 'Old state vars still present'
print('OK: syntax valid, old state variables removed')
"</automated>
  </verify>
  <done>Edit form fetch description writes directly to widget key. Description appears in textarea immediately after fetch, and persists correctly on save. Old edit_fetched_description and fetched_description session state variables are fully removed.</done>
</task>

</tasks>

<verification>
1. Run the app: `cd /Users/alec/git/toma && streamlit run app.py`
2. Add flow: Enter item name, click Fetch Description -- description appears in textarea before submitting
3. Edit flow: Click edit on existing item, click Fetch Description -- description appears in textarea
4. Both flows: submit form, verify description persists on the card
</verification>

<success_criteria>
- Fetched Wikipedia descriptions visually populate the textarea immediately (no phantom success message)
- No separate lookup field confusion (uses main Name field)
- Both add and edit flows work identically
- Old fetched_description / edit_fetched_description session state variables removed
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhancements/03-03-SUMMARY.md`
</output>
