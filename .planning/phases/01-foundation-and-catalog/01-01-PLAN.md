---
phase: 01-foundation-and-catalog
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - db.py
  - models.py
  - services/__init__.py
  - services/item_service.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_db.py
  - tests/test_item_service.py
autonomous: true
requirements:
  - DATA-01
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
  - CAT-05
  - CAT-09
  - CAT-11

must_haves:
  truths:
    - "Items persist in a SQLite file across app restarts"
    - "User can create an item with name, category, default dosage, unit, and notes"
    - "User can update any field on an existing item"
    - "User can soft-delete an item (is_active=0) and it no longer appears in queries"
    - "User can search items by name substring"
    - "User can filter items by category"
  artifacts:
    - path: "db.py"
      provides: "SQLite connection management and schema initialization"
      exports: ["get_connection", "init_db"]
    - path: "services/item_service.py"
      provides: "Catalog CRUD operations"
      exports: ["create_item", "get_item", "get_active_items", "update_item", "deactivate_item", "search_items", "get_categories"]
    - path: "tests/test_item_service.py"
      provides: "CRUD operation tests"
      min_lines: 80
    - path: "tests/test_db.py"
      provides: "Schema and persistence tests"
      min_lines: 20
  key_links:
    - from: "services/item_service.py"
      to: "db.py"
      via: "get_connection context manager"
      pattern: "from db import get_connection"
    - from: "tests/conftest.py"
      to: "db.py"
      via: "in-memory SQLite fixture overriding DB_PATH"
      pattern: "init_db|DB_PATH"
---

<objective>
Set up the Toma project and build the complete data layer with TDD: SQLite database schema, connection management, and item_service CRUD operations covering all Phase 1 backend requirements.

Purpose: Everything in Phase 1 depends on a working, tested data layer. Building this first with TDD gives confidence that CRUD operations work correctly before wiring up the UI.
Output: Working pyproject.toml, db.py, item_service.py, and a green test suite covering all catalog operations.
</objective>

<execution_context>
@/Users/alec/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-catalog/01-RESEARCH.md
@.planning/phases/01-foundation-and-catalog/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project and create database layer</name>
  <files>pyproject.toml, db.py, models.py, services/__init__.py, services/item_service.py, tests/__init__.py, tests/conftest.py</files>
  <action>
1. Create pyproject.toml with project metadata and dependencies:
   - Runtime: streamlit>=1.55, pandas>=2.2
   - Dev: pytest, ruff
   - Add [tool.pytest.ini_options] with testpaths = ["tests"]

2. Run `uv init --no-readme` if needed, then `uv add streamlit pandas && uv add --dev pytest ruff` to install dependencies.

3. Create db.py with:
   - DB_PATH = Path(__file__).parent / "toma.db"
   - get_connection() context manager: connects to DB_PATH, sets row_factory=sqlite3.Row, enables WAL mode and foreign keys, yields conn, commits on success, rollbacks on exception, closes in finally
   - init_db(): creates items table with schema from research (id, name UNIQUE, category DEFAULT 'supplement', default_dosage REAL, dosage_unit DEFAULT 'mg', description DEFAULT '', notes DEFAULT '', sort_order DEFAULT 0, is_active DEFAULT 1, created_at, updated_at), plus indexes on category and is_active

4. Create models.py with an Item dataclass:
   - Fields: id (int|None), name (str), category (str), default_dosage (float|None), dosage_unit (str), description (str), notes (str), sort_order (int), is_active (bool), created_at (str|None), updated_at (str|None)
   - Class method from_row(row: sqlite3.Row) -> Item

5. Create services/__init__.py (empty) and services/item_service.py with stubs for: create_item, get_item, get_active_items, update_item, deactivate_item, search_items, get_categories. Each stub should raise NotImplementedError("TODO").

6. Create tests/__init__.py (empty) and tests/conftest.py with:
   - A pytest fixture `db` that patches db.DB_PATH to a temp file, calls init_db(), yields, then cleans up the temp file. This ensures tests use an isolated database.
  </action>
  <verify>
    <automated>cd /Users/alec/git/toma && uv run python -c "from db import get_connection, init_db; from services.item_service import create_item; print('imports OK')"</automated>
  </verify>
  <done>Project structure exists, dependencies installed, db.py creates schema, item_service.py has stubs, conftest.py provides test fixture</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — database persistence and all CRUD operations</name>
  <files>tests/test_db.py, tests/test_item_service.py, services/item_service.py</files>
  <behavior>
    tests/test_db.py:
    - test_init_db_creates_items_table: after init_db(), items table exists in SQLite
    - test_persistence: create item, close connection, reopen, item still there

    tests/test_item_service.py:
    - test_create_item: create_item("Vitamin D", "supplement", 5000.0, "IU", "Daily") returns int id > 0
    - test_create_item_duplicate_name: creating same name twice raises IntegrityError
    - test_get_item: get_item(id) returns dict with all fields matching what was created
    - test_get_active_items: returns only items with is_active=1, ordered by name
    - test_update_item: update_item(id, name="New Name") changes name, updated_at changes
    - test_deactivate_item: deactivate_item(id) sets is_active=0, item no longer in get_active_items()
    - test_item_category: created item has correct category; update_item can change category
    - test_default_dosage: created item stores dosage and unit correctly; both nullable
    - test_item_notes: create with notes, update notes, verify notes field
    - test_search_items_by_name: search_items(query="vita") matches "Vitamin D" but not "Fish Oil"
    - test_search_items_by_category: search_items(category="prescription") returns only prescription items
    - test_search_items_combined: search_items(query="vita", category="supplement") filters by both
    - test_get_categories: returns distinct category list from active items only
  </behavior>
  <action>
Follow RED-GREEN-REFACTOR for each test group:

RED PHASE: Write all tests in test_db.py and test_item_service.py first. Run `uv run pytest tests/ -x` — all tests must FAIL (NotImplementedError from stubs). Commit: "test(01-01): add failing tests for db and item_service"

GREEN PHASE: Implement services/item_service.py functions:
- create_item(name, category, default_dosage, dosage_unit, notes=""): INSERT into items, return lastrowid
- get_item(item_id): SELECT by id, return dict(row) or None
- get_active_items(): SELECT WHERE is_active=1 ORDER BY name, return list[dict]
- update_item(item_id, **kwargs): UPDATE only provided fields, set updated_at=datetime('now')
- deactivate_item(item_id): UPDATE is_active=0, updated_at=datetime('now')
- search_items(query="", category=""): Build dynamic WHERE clause with LIKE for name and = for category, return list[dict]
- get_categories(): SELECT DISTINCT category WHERE is_active=1

All functions use `from db import get_connection` with the context manager pattern.

Run `uv run pytest tests/ -v` — all tests must PASS. Commit: "feat(01-01): implement db and item_service CRUD"

REFACTOR (if needed): Clean up any duplication. Tests must stay green.
  </action>
  <verify>
    <automated>cd /Users/alec/git/toma && uv run pytest tests/ -v</automated>
  </verify>
  <done>All 15+ tests pass. create_item, get_item, get_active_items, update_item, deactivate_item, search_items, get_categories all work correctly. Data persists in SQLite file.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v` — all tests green
- `uv run python -c "from db import init_db; init_db(); print('schema OK')"` — no errors
- `uv run python -c "from services.item_service import create_item, get_active_items; from db import init_db; init_db(); create_item('Test', 'supplement', 100.0, 'mg'); print(get_active_items())"` — prints item list
</verification>

<success_criteria>
- All CRUD operations work: create, read, update, soft-delete
- Search by name and filter by category work
- All data persists in toma.db SQLite file
- Test suite has 15+ tests, all green
- No Streamlit imports in db.py or services/ (clean separation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-catalog/01-01-SUMMARY.md`
</output>
